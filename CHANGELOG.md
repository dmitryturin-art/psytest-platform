# Журнал изменений PsyTest

Этот файл написан для владельца продукта. Здесь фиксируются только завершённые и проверенные изменения, которые меняют продукт, безопасность, эксплуатацию или процесс разработки. Технические подробности находятся в `docs/roadmap/WORKLOG.md`.

## Не выпущено

- В результате СМИЛ 75 дополнительных шкал вместо 55: добавлены шкалы гипомании (чистая, аморальность, психомоторная акселерация, явная и скрытая) и социального функционирования (контроль, соперничество, лидерство, альтруизм, социальная желательность, социальный статус, застенчивость и другие); все с пояснениями для ИИ-разбора.

## 2026-09-15 (утро следующего дня) — обновление рабочего тестового стенда

- В карточке кейса появился блок «Выгрузка»: PDF и версия для печати с результатом, всеми шкалами, профессиональным заключением и понятным разбором; по желанию с анкетой и заметкой. Статусы в кабинете переведены на русский. В PDF результата исправлена раскладка профиля, растянутого на много страниц.

## 2026-09-15 (поздняя ночь) — обновление рабочего тестового стенда

- Обработчик разборов вычерпывает всю очередь, а не только своё задание; если задание ждёт без обработчика, открытие карточки кейса запускает его заново.

## 2026-09-15 (ночь) — обновление рабочего тестового стенда

- Готовый черновик тоже можно заказать заново: новый текст модели ложится следующей версией, правки специалиста и опубликованная версия сохраняются. Перезаказывается только выбранный вид разбора.
- (пусто)
- СМИЛ: добавлены ещё 20 проверенных дополнительных шкал (всего 55): подшкалы паранойи, психопатии и шизофрении по Харрису-Лингоузу и Винеру-Хармону, а также исторические исследовательские шкалы с пометкой, что они не являются диагнозом. Пояснения для ИИ покрывают все 55 шкал.

## 2026-09-15 (вечер, пятый релиз) — обновление рабочего тестового стенда

- «Заказать заново» готовит разбор по текущим данным, пояснениям и опубликованному промпту, а не по снимку первоначального заказа.
- Кнопки «Заказать заново», «Обновить», «Уведомить клиента» и действия в редакторе стали заметными: основное действие синее, второстепенные с синей рамкой.

## 2026-09-15 (вечер, четвёртый релиз) — обновление рабочего тестового стенда

- В визуальном редакторе разбора больше не залипает список заголовков при правке. Зависшее задание разбора возвращается в очередь и при просмотре карточки, а не только при новом заказе.

## 2026-09-15 (вечер, третий релиз) — обновление рабочего тестового стенда

- В карточке кейса черновик, у которого исчерпаны попытки, можно заказать заново явной кнопкой; данные для модели остаются теми же.

## 2026-09-15 (вечер, второй релиз) — обновление рабочего тестового стенда

- Редактор понятного разбора стал визуальным: таблицы, списки и заголовки редактируются как в текстовом редакторе, с переключением в Markdown. Кнопка «Обновить» в карточке кейса перезагружает страницу, а автообновление срабатывает, как только готов любой из черновиков.

## 2026-09-15 (ночь) — обновление рабочего тестового стенда

- Заказ черновиков из кабинета и разбора со страницы результата отвечает сразу: обработка идёт отдельным процессом на сервере. В карточке кейса после заказа показывается индикатор ожидания, статус обновляется сам, при ошибке есть кнопка «Повторить». Исправлено падение «MySQL server has gone away» при сохранении готового разбора.
- ИИ-разбор СМИЛ получает пояснения по всем 35 дополнительным шкалам.
- В карточке кейса ответы по пунктам свёрнуты по умолчанию, в заголовке число ответов.

## 2026-09-15 (поздний вечер) — обновление рабочего тестового стенда

- ИИ-разбор СМИЛ получает пояснения по дополнительным шкалам первой партии и правило уровней; шкалы без пояснения перечисляются явно, чтобы модель не додумывала. Новые версии промптов лежат черновиками в разделе «Промпты» и включаются специалистом.
- СМИЛ: добавлены ещё 19 проверенных дополнительных шкал (всего 35): подшкалы депрессии, истерии, ипохондрии, тревоги, невротизма и соматизации по Собчик (2003). Нумерация пунктов сверена с оригинальными западными ключами MMPI.

## 2026-09-15 (вечер) — обновление рабочего тестового стенда

- СМИЛ: дополнительные шкалы заменены на 16 шкал, проверенных по книге Л.Н. Собчик (2003): ключи перенесены со скана двумя независимыми прочтениями, нормы по полу, расчёт сверен с независимым эталоном. Прежние 23 неподтверждённые шкалы убраны из результата и PDF. Базовые шкалы и график не менялись.

## 2026-09-15 (день) — обновление рабочего тестового стенда

- В кабинете появился раздел «Промпты»: можно посмотреть версии промптов для каждой методики, создать свою версию, проверить её на обезличенном примере, опубликовать и откатить. Там же выключатель ИИ-разборов и выбор модели.
- Письмо «Ваш разбор готов» содержит ссылку на страницу результата клиента и предупреждение не пересылать её; имени и текста разбора в письме нет.
- Парный Лазарус: у второго партнёра страница результата и PDF больше не подписывают его оценки как «Начавший»; своя карточка помечена «это вы».
- У сайта появилась иконка во вкладке браузера.
- При удалении клиента или аккаунта файлы PDF удаляются только после успешного удаления записей.

## 2026-09-15 (ночь) — обновление рабочего тестового стенда

- Карточка кейса в кабинете для парного прохождения Лазаруса показывает парный результат: график совмещённых профилей, сравнение и анкеты обоих партнёров, с пометкой, какой партнёр это кейс. Индивидуальный результат свёрнут ниже.

## 2026-09-15 — обновление рабочего тестового стенда

- В кабинете найденную по ссылке результата сессию можно привязать к карточке клиента (существующего или нового). Кейс сразу появляется в карточке клиента и открывается как обычный кейс с разделом «ИИ-разбор».

## 2026-09-14 (поздняя ночь) — обновление рабочего тестового стенда

- В карточке клиента можно указать email (необязательно). После публикации разбора кнопка «Уведомить клиента на email» отправляет короткое письмо без текста разбора и без ссылок: клиенту предлагается открыть свою страницу результата. Не чаще раза в 10 минут.
- Черновики, заказанные из кабинета специалиста, теперь готовятся сразу после заказа, без отдельного обработчика на сервере.
- В карточке кейса по методикам без настроенных промптов раздел «ИИ-разбор» показывает пояснение вместо того, чтобы исчезать.

## 2026-09-14 (ночь) — обновление рабочего тестового стенда

- Кабинет специалиста: для кейса по приглашению можно заказать черновики разбора, править понятную версию с историей версий и опубликовать её. Клиент видит одобренный разбор на своей странице результата и в PDF только после публикации; черновики ему не показываются.

## 2026-09-14 (поздний вечер) — обновление рабочего тестового стенда

- Заказанный ИИ-разбор теперь готовится ровно по тем данным и той версии промпта, что были в момент заказа: более поздняя правка промпта или данных не меняет уже поставленный разбор.

## 2026-09-14 (вечер) — обновление рабочего тестового стенда

- Личный кабинет посетителя по желанию: вход по одноразовой ссылке на email (без пароля), кнопка «Сохранить в мой кабинет» на странице результата, список сохранённых результатов, отвязка и полное удаление аккаунта вместе с результатами. Ничего не сохраняется автоматически; сохранённые результаты не удаляются автоматической очисткой. Политика приватности дополнена.

## 2026-09-14 — обновление рабочего тестового стенда

- Кабинет специалиста: карточки клиентов. Для клиента заводится подпись (видна только владельцу), делаются несколько назначений на разные методики, видны состояние каждого и история результатов; клиент удаляется вместе со всеми результатами. Контакты клиента не хранятся.
- Кейс по приглашению удаляется прямо из его карточки. Заметка к приглашению не остаётся после удаления результата, в том числе когда клиент удаляет результат сам.
- СМИЛ открывается только по личному приглашению из кабинета; общий ключ в адресе больше не действует.
- После исчерпания попыток прерванный ИИ-разбор получает статус ошибки вместо бесконечного ожидания.
- Команда «Продолжай PsyTest» направляет агента к конкретному следующему пакету в START_HERE.md.

## 2026-09-13 — обновление рабочего тестового стенда

- В кабинете специалиста можно создать одну ограниченную по сроку ссылку-приглашение на любую поддерживаемую методику, отозвать неоткрытую ссылку и затем увидеть назначенный кейс. Контактные данные клиента для этого не требуются.
- После завершения теста повторная отправка формы больше не может изменить сохранённые ответы или результат.


### Управление разработкой

- Начата единая поэтапная программа развития на основе независимого аудита от 15 августа 2026 года.
- Superpowers перестал быть обязательной методологией проекта.
- Зафиксированы продуктовые правила: бесплатные тесты и базовые результаты, платный расширенный разбор только отдельных сложных методик, купоны для клиентов терапевта и три формы отчёта.
- Канонический график SMIL объявлен защищённым элементом: L/F/K, разрыв и шкалы 1–9/0 нельзя заменять другой диаграммой.
- Введены отдельные правила веток, коммитов, проверок, контрольных точек и трассировки замечаний аудита.
- Добавлена экономная оркестрация: независимые проверки можно выполнять параллельно быстрыми субагентами, а рискованные решения и финальную сборку контролирует ведущий агент.
- Git перестаёт отслеживать сгенерированные PDF результатов и старые локальные browser/debug-снимки; локальные копии при этом сохранены.
- Обновлённый roadmap и README опубликованы в публичном GitHub-репозитории.
- Проверка архитектуры теперь действительно запускается из корня проекта, проверяет все пять текущих тестовых модулей и возвращает ошибку при сбое.
- Добавлена защита от незаметного роста временного PHPStan baseline: сейчас он ограничен 148 известными сообщениями.
- Проверен будущий закрытый staging на Beget: PHP 8.3 и отдельный web root подходят проекту; приложение ещё не выложено, пока не готовы HTTPS и проверка MySQL 5.7.
- Исправлена маршрутизация для размещения сайта прямо в выделенном web root Beget: внутренние страницы больше не направляются в несуществующий вложенный каталог `public`.
- Миграции и весь quality gate теперь автоматически проверяются на MySQL 5.7 и 8.0; найденная несовместимость полей срока действия с базой Beget исправлена до выкладки.
- CI больше не повторяет PHPStan, форматирование и все обычные тесты для каждой версии MySQL: общий gate выполняется один раз, а дорогая DB-матрица остаётся для изменений слоя данных и каждого обновления `main`.
- На Beget загружена, но ещё не включена проверенная staging-сборка: текущая публичная заглушка сохранена и имеет отдельную резервную копию. Переключение ждёт HTTPS.
- Тестовый стенд запущен на `test.23time.ru`: работает только по HTTPS, каталог и бесплатные результаты доступны; реальный мобильный BDI-сценарий 21/21 сформировал результат без прежней ошибки ответов. Оплата, AI и кабинет владельца выключены.
- Тестовый стенд обновлён: сессионная cookie защищена флагами `Secure`, `HttpOnly` и `SameSite`, а повторяющиеся заголовки безопасности устранены без изменения тестов и результатов.
- Подготовлены три интерактивных прототипа будущего лендинга и каталога для выбора визуального направления; они пока не подключены к тестовому стенду и не меняют страницы результатов.
- Добавлен контроль свежести локальной карты проекта: следующий агент видит, когда Graphify устарел, и не должен опираться на старую карту без явной отметки.
- На тестовом стенде обновлены главная и каталог тестов в выбранной «тёплой редакционной» стилистике. Все пять методик по-прежнему ведут к бесплатному прохождению; прохождение тестов, результаты и график СМИЛ этим обновлением не менялись.
- На телефоне опросник Лазаруса получил более крупную и точную сетку ответов 5×2. При переходе между вопросами страница больше не возвращает человека к заголовку; кнопка возврата теперь называется «Предыдущий вопрос».
- В парном результате Лазаруса появились две понятные шкалы общей оценки отношений, а сравнение теперь прямо объясняет, чьи оценки сопоставляются и что означает «насколько угадал». На телефоне подробные 16 пунктов открываются как компактные карточки, а не как широкая нечитаемая таблица.
- Парный результат Лазаруса стал аккуратнее: шкала совпадения теперь выглядит так же, как остальные шкалы результата, а подробное сравнение открывается заметной кнопкой. PDF сравнения стал альбомным и использует компактную таблицу с ясными сокращениями, поэтому все шесть столбцов помещаются на странице.
- Исправлен пропущенный случай в PDF Лазаруса: при скачивании полного результата с парным сравнением теперь действительно используется компактная альбомная таблица. Раньше этот маршрут терял PDF-режим и печатал широкую web-версию, из-за чего документ выглядел сломанным.

### Код продукта

- На странице результата появилась кнопка «Получить разбор»: подробный разбор готовится в фоне несколько минут, страницу можно закрыть и вернуться позже. Доступны два варианта — понятный и профессиональное заключение.
- После нажатия «Получить разбор» страница больше не выглядит зависшей: кнопка сразу показывает, что запрос ушёл, а страница результата возвращается мгновенно с сообщением об ожидании и крутилкой. Раньше браузер несколько минут ждал завершения работы модели и всё это время показывал пустую загрузку.
- СМИЛ больше не публикуется открыто: методика исчезла из общего каталога, а её страница без личной ссылки отвечает «не найдено». Ссылку для своих клиентов владелец получает командой и может перевыпустить в любой момент. Остальные тесты работают как прежде.
- Исправлено положение отметки на шкале результата: в тесте Лазаруса она стояла правее, чем следует из баллов, и при неудовлетворённости могла оказаться в зелёной зоне. Сами баллы и уровни не менялись — только положение метки на полосе.
- Кабинет владельца открыт и работает на `test.23time.ru`: вход по паролю, поиск сессии по ссылке или токену, выход. Пароль меняется одной командой и одной строкой в настройках сервера — процедура описана в `DEVELOPMENT.md`.
- Устранены известные уязвимости библиотеки генерации PDF: Dompdf обновлён до безопасной версии, а набор зависимостей теперь фиксируется в `composer.lock`.
- Минимальная версия PHP для развёртывания теперь 8.3; набор зависимостей проверяется именно относительно этой границы.
- Старые кнопки и endpoints платной AI-интерпретации временно отключены, чтобы не было сломанных оплат или скрытого legacy-обработчика до внедрения YooKassa.
- Ссылка на результат теперь работает только с тем тестом, для которого она создана; подмена адреса другого теста не раскрывает и не смешивает данные.
- Сервер отклоняет неполные и недопустимые ответы до сохранения результата для всех имеющихся методик.
- Полностью заполненный тест депрессии Бека снова завершается корректно: числовые номера вопросов больше не теряются при отправке формы.
- Индикатор прохождения Бека теперь после последнего ответа честно показывает `21 из 21` и заполненную шкалу, а не останавливается на `20 из 21`.
- На первом вопросе больше нет пустой липкой панели: навигация появляется только тогда, когда доступна кнопка «Назад» или завершение теста.
- Для новых прохождений прекращён автоматический сбор IP-адреса и сведений о браузере; технические события сохраняются без этих идентификаторов.
- Подготовлен серверный барьер будущей AI-интерпретации: бесплатное прохождение само по себе не создаёт согласия, а разрешение должно быть отдельным и привязанным к конкретному оформлению, провайдеру и составу данных.
- Подготовлен простой регламент закрытого бесплатного пилота: что именно проверять, какие функции не включать, как записывать замечания без клиентских данных и когда немедленно останавливать тестирование.
- После положительного ответа на 9-й пункт Бека на странице результата появляется утверждённое нейтральное сообщение о том, как обратиться за поддержкой. В нём нет неподтверждённых телефонов, ссылок или региональных рекомендаций.
- Добавлена автоматическая проверка итоговой HTML-страницы Бека: предупреждение не должно исчезнуть, превратиться в ссылку или оказаться ниже обычных действий, а при нулевом ответе оно не показывается.
- Парное приглашение Лазаруса нельзя использовать повторно; ограничение проверяется в коде и базе данных.
- Исправлена цепочка миграций: новая база данных разворачивается без конфликта индексов и прошла полный PHP 8.3/MySQL quality gate.
- Из production web root убраны диагностические страницы и генератор тестовых сессий; доступны только рабочие маршруты приложения.
- Парный сценарий Лазаруса дополнительно защищён: вторую сессию нельзя подменить сессией из другого приглашения.
- Парное приглашение теперь безопасно истекает и корректно обрабатывает одновременное открытие ссылки; технические ошибки базы не выводят token в логи.
- Анонимные результаты теперь имеют отдельный срок хранения 180 дней: плановая очистка не затрагивает явно назначенные кейсы терапевта и удаляет известные PDF, парные данные и привязанные технические записи вместе с сессией.
- Добавлен небольшой защищённый кабинет владельца: в нём можно явно назначить завершённый результат кейсом клиента и затем удалить такой кейс вместе с известными файлами. Кабинет выключен, пока на сервере не настроен отдельный пароль владельца; он не является ещё полным кабинетом с ИИ-разборами.
- Публичная страница об обработке данных и тексты удаления теперь не обещают шифрование, отсутствие будущих внешних получателей или мгновенное физическое удаление того, чего текущий код не делает.
- Для всех пяти текущих методик создан проверяемый реестр происхождения и прав использования. Он честно фиксирует отсутствие документального подтверждения по конкретным формам и не позволяет считать платную интерпретацию готовой до проверки.
- Инструкции для разработчиков теперь отделяют работающие бесплатные сценарии от отключённых старых AI/YooMoney-маршрутов и не предлагают несуществующие команды или удалённые публичные страницы.

## Как вести этот файл

- Запись переносится из `Не выпущено` в датированный релиз только после фактической выкладки.
- Формулировки отвечают на вопрос «что изменилось для владельца или пользователя», а не перечисляют классы и методы.
- Исправленная ошибка описывается вместе с видимым эффектом.
- Незавершённая работа остаётся в `WORKLOG.md`/`STATUS.md`, но не выдаётся здесь за готовую функцию.

# Текущий статус программы

Обновлён: 2026-08-25. Это единственная оперативная панель; порядок фиксации перед паузой находится в [CHECKPOINT.md](CHECKPOINT.md). Панель синхронизирована с новейшими записями [WORKLOG.md](WORKLOG.md); первоисточник хронологии — WORKLOG.

## Сейчас

- Активные этапы: [02 — клиническая безопасность и бесплатный пилот](phases/02-clinical-privacy-pilot.md) + [07 — ИИ-разборы и кабинет терапевта](phases/07-ai-reports-therapist-office.md) + [08 — рабочий сайт](phases/08-production-deployment.md). Этапы 00 и 04 закрыты 25.08; по 03 все exit criteria выполнены (03.4 закрыл walkthrough), нужно решение владельца о закрытии этапа.
- Состояние: `test.23time.ru` работает на release `0687412` (выложен 27.08, пакет 08.4; предыдущий `78c5c4e` сохранён как точка отката) через стабильную точку `current`: HTTPS redirect, PHP 8.3, MySQL 5.7, 9 migrations. Сессионная cookie имеет `Secure`, `HttpOnly`, `SameSite=Lax`; динамические security headers не дублируются. Откат — на предыдущий релиз через `releases/`. Направление A целиком на staging: главная, каталог, мобильное прохождение, веб-график совмещённых профилей пары и compact PDF; cleanup-cron (03:17) настроен владельцем, restore drill пройден, `PRODUCTION_RUNBOOK.md` готов.
- Этап 03, пакет 03.5: устойчивость `ModuleLoader` — сломанный модуль больше не обрушивает каталог (`\Throwable`), чужая директория в `modules/` не логируется как дефект, кэш APCu привязан к modules-пути и принимается только после проверки живых инстансов; удалён мёртвый `Security::sanitizeHtml()` с небезопасным allowlist. Проверено на сервере 26.08: APCu на Beget для PHP 8.3 не установлен (`extension_loaded("apcu") === false`, модуля нет в extension_dir), поэтому кэш-ветка загрузчика в production мертва и риска устаревания путей после выкладки нет; отдельный пакет по кэшированию не нужен.
- Этап 03, пакет 03.4: последний exit criterion закрыт — walkthrough `creating-new-test.md` выполнен на чистом клоне со сквозным прогоном (каталог → прохождение → HTML-результат → PDF) без правок общего слоя. Руководство содержало четыре дефекта (неработающий сниппет миграции Phinx, пропущенную обязательную запись в реестре методик, ложную инструкцию про `bin/check-architecture.php`, пропущенный шаг PSR-4) — все исправлены и закреплены text-contract тестом. Этап 03 готов к владельческому решению о закрытии.
- Governance 00N: остаточная поверхность Superpowers убрана — `docs/superpowers/` перенесён в `docs/archive/superpowers/` со ссылками в AGENTS.md/ROADMAP.md, локальные артефакты и отключённый плагин удалены из окружения. Методология не действует с этапа 00; изменений в коде нет.
- Этап 02, пакет 02.8: **кабинет владельца работает на `test.23time.ru`** — включён Argon2id-хэшем в серверном `.env` без выкладки нового кода (код был в релизе `c62f34a`). Проверено живым HTTPS-прогоном: вход, страница кабинета, отклонение неверного пароля (422), выход. Смена пароля — `php bin/owner-password.php` и одна строка в `.env` (процедура в DEVELOPMENT.md). Пароль выбран простой по решению владельца, сменить перед пилотом с реальными клиентами.
- Этап 08, пакет 08.2: релиз `db922e8` выложен на `test.23time.ru` по PRODUCTION_RUNBOOK — pre-deploy дамп снят, миграции no-op, переключение атомарное, откат на `c62f34a` готов. Проверено внешним HTTPS: все публичные страницы 200, сквозной прогон HADS до PDF, кабинет владельца работает, логи чисты, данные целы. Долг: `bin/build-release.sh` собирает артефакт из рабочего дерева и затащил в релиз 2,2 МБ отладочных скриншотов из корня репозитория — собирать нужно из git-дерева.
- Инструментарий 00P: полный gate теперь запускается локально одной командой `bin/local-gate.sh` — статический контур плюс миграции и PHPUnit на MySQL 5.7 в Docker, то есть на версии сервера (`test.23time.ru`: MySQL 5.7.21, PHP 8.3.20; локально у разработчика MySQL 9.6). 22 секунды в тёплом прогоне, 6 секунд в режиме `--fast` без Docker. GitHub Actions остаётся независимой записью в PR, но ожидание его больше не блокирует работу; открытый вопрос — сокращать ли облачную матрицу.
- Дефект 04.1 (найден владельцем 26.08, исправлен): маркер на шкале результата считался от нуля, а цветные зоны рисуются от минимума первой зоны. У Лазаруса шкала начинается с 16, поэтому баллы 71–79 при уровне «неудовлетворённость» рисовались внутри зелёной зоны. Остальные методики не затронуты — их шкалы начинаются с нуля. Scoring не менялся.
- **Этап 07 начат** (пакет 07.1): введён контракт `aiReportContext()` — модуль сам объявляет, что уходит внешнему ИИ, по умолчанию не уходит ничего. Lazarus отдаёт индивидуальную и парную нагрузку по документу промптов; контракт-тест стережёт отсутствие идентификаторов, демографии и HTML. Исходящих вызовов к ИИ в коде пока нет. Ждут владельческих решений: провайдер/модель, потолок расходов на разбор, промпт для СМИЛ.
- Кабинет владельца: доработка (ссылка на результат, русские подписи статусов) по решению владельца от 26.08 отнесена в этап 07 WP6 — делается вместе с нормальным кабинетом, отдельным пакетом раньше не выносится.
- Этап 07, пакет 07.2: реестр промптов введён — версии, статусы, откат по номеру версии; шесть ключей (четыре Лазаруса, два СМИЛ) **все в статусе draft**, боевой поток их не получит до одобрения владельцем. Профессиональный промпт СМИЛ собран из промпта владельца; понятный вариант СМИЛ — черновик, владельцем не читался. Исходящих вызовов к ИИ в коде нет.
- Открытые вопросы дизайна этапа 07 (в phase-файле): возраст для СМИЛ (нужен ИИ, в scoring не идёт), поле клинического контекста в кабинете и его судьба по §11, третий режим Лазаруса `pair_personal`.
- Этап 07, пакет 07.3: шесть промптов **одобрены владельцем и опубликованы** (D-038); понятный вариант СМИЛ дополнен разбором дополнительных шкал по замечанию владельца. Провайдер выбран (D-039): OpenRouter, на старте бесплатная модель, адаптер вокруг OpenAI-совместимого endpoint — смена провайдера без правки кода, модель из `.env`, позже из кабинета. Открытый вопрос приватности: бесплатные модели обычно предполагают обучение на запросах.
- Этап 07, пакет 07.4: адаптер провайдера готов — OpenAI-совместимый `chat/completions`, настройки `AI_*` с чтением исторических `OPENROUTER_*`, каталог моделей для выпадающего списка в кабинете, фактически ответившая модель пишется отдельно от запрошенной. Ключ владельца внесён в локальный `.env`, модель переключена на `openrouter/free`. Продуктовый поток вызовов ещё не делает.
- Этап 07, пакет 07.5: **первый живой вызов провайдера выполнен, формат входа подтверждён** — модель поняла структурированный JSON и дала связный разбор с точными числами. Но бесплатные модели непригодны: маршрутизатор `openrouter/free` отдал запрос классификатору безопасности, `:free`-эндпоинты массово отвечают 429 из общего пула, а ответившая модель замусорила русский текст иноязычными словами. Добавлены повторы с растущей паузой. Сравнение с ручным способом владельца нужно делать на платной модели.
- Этап 07, пакеты 07.7–07.8: возраст при прохождении **не спрашивается** (D-040). Форму методики СМИЛ задаёт выбор респондента — мужчина, женщина, мальчик, девочка; подростковые варианты для 13–15 лет отличаются только формулировкой вопросов. Возраст уходит в блок дополнительных сведений при заказе разбора. Общий слой при этом научился проверять возраст, если методика его объявит: до этого поле проходило валидатор насквозь. Подростковый набор вопросов не заведён — отдельный пакет с подтверждением прав; открытый вопрос владельцу: одинаковы ли нормы T-баллов для форм.
- Обзор источников 05.0 (`docs/smil-sources-review.md`): подростковой формы СМИЛ в наших источниках **нет** — руководство Собчик её не содержит, остальные упоминают подростков только как поправку к интерпретации взрослого профиля; строить подростковый вариант сейчас не на чем. Зато найдено, что **полные ключи дополнительных шкал с нормами для мужчин и женщин лежат в Приложении Собчик** (номера до 206+, ориентировочно с. 195–222): разрыв «23 реализовано против 200 заявленных» объясняется тем, что источник — скан без текстового слоя, а не отсутствием данных.
- **Этап 03 закрыт** (26.08, решение владельца): все пять exit criteria выполнены. Стек из семи веток этапов 05 и 07 слит в `main`, открытых PR нет. D-041: рабочая модель `qwen/qwen3.7-plus`, выбор не окончательный — модель меняется значением `AI_MODEL` без правки кода, DeepSeek рассматривается как замена.
- Этап 07, пакет 07.9: **нагрузка СМИЛ для ИИ готова** — только уровень шкал. Формулировки пунктов, ответы по пунктам, наши собственные интерпретирующие тексты и нормативные константы наружу не уходят, это закреплено тестами. Расчёт не тронут: в модуле только вставки, ноль удалений. Живые прогоны обоих промптов СМИЛ на `qwen/qwen3.7-plus` дали отчёты 10 074 и 8 180 символов со структурой точно по промпту.
- Разведка 09.0 (`docs/open-instruments-review.md`): проверено у источников — **IPIP** в общественном достоянии с явным разрешением на коммерцию (открытый аналог 16PF и Big Five), **PHQ-9/GAD-7** свободны для использования, **HADS** лицензируемая и платная, **CSI** свободен по форме авторов. Найдено, что закрытие СМИЛ не решает вопрос целиком: HADS, BDI и BAI остаются публичными и несвободными. Платный контур разумно строить на IPIP.
- Этап 02, пакет 02.10 (D-043): **СМИЛ закрыт ссылкой-приглашением** — в каталоге не показывается, страница без ключа отвечает 404. Публичного распространения 566 формулировок больше нет. Ссылка выдаётся `bin/test-access-link.php`, ключ перевыпускается там же. Это шлюз, а не опознание человека: персональные приглашения — часть купонного контура этапа 06. Лазарус остаётся открытым до появления аналога.
- Спроектированы приглашения клиентов (`docs/roadmap/INVITE_FLOW.md`, этап 07 WP6): создание в кабинете, подпись для узнавания клиента, отправка средствами владельца (копирование, почта, Telegram, Max) — **платформа не хранит контакты клиентов**, привязка прохождения к приглашению автоматическая, состояние видно в кабинете. Нынешний общий ключ и `bin/test-access-link.php` признаны затычкой и удаляются вместе с реализацией.
- Открытые аналоги внесены в план этапа 09: IPIP вместо СМИЛ и 16PF, PHQ-9 вместо BDI, GAD-7 вместо BAI, HADS заменить или убрать, CSI как пара к Лазарусу. Реализация — после того, как заработает основной контур.
- Этап 07, пакет 07.11: заложен фундамент видимого разбора — таблица `ai_reports` (с каскадным удалением вместе с сессией) и рендерер Markdown, который сначала экранирует весь ответ модели и только потом добавляет свои теги; ссылок и картинок не создаёт намеренно. Осталось: фоновая генерация и показ на странице результата.
- Этап 07, пакет 07.12: **фоновая генерация разборов работает** — очередь с атомарным захватом, возвратом зависших заданий и ограничением попыток, обработчик для cron. Сквозная проверка на живом провайдере пройдена. Модель по умолчанию заменена с маршрутизатора на конкретную: `openrouter/free` во второй раз отдал клинический разбор модели не того назначения — сначала классификатору безопасности, теперь модели для кода.
- **Блокер этапа 07 (27.08): OpenRouter недоступен с рабочего сервера.** Ответ 403 «Access denied by security policy» приходит и вовсе без ключа, то есть провайдер закрывает доступ с адреса хостинга. Исходящий HTTPS в порядке, ключ цел, локально всё работает. С сервера доступны `api.deepseek.com`, `dashscope-intl.aliyuncs.com` (Qwen от Alibaba), `api.openai.com`, `api.proxyapi.ru`, `api.vsegpt.ru`. **Смена провайдера правки кода не требует** — нужны только `AI_BASE_URL` и `AI_API_KEY`. Ждёт решения владельца.
- Этап 07, пакет 07.13: найдена и устранена причина, по которой первый разбор на сервере не сохранился — `wait_timeout` MySQL на Beget равен 30 секундам, а вызов модели идёт больше двух минут, и соединение умирало до записи результата. `Database::execute()` восстанавливает соединение и повторяет запрос; внутри транзакции отказ пробрасывается, но объект больше не остаётся непригодным.
- Этап 07, пакеты 07.15–07.16: **разбор заказывается кнопкой на странице результата**, готовый сворачивается в раскрывающийся блок, ожидание показано отдельным состоянием. **Расписание не нужно:** заказ отвечает браузеру сразу и доготавливает разбор в том же процессе; прерванный прогон возвращается в очередь сам. Работоспособность на Beget проверяется выкладкой.
- Этап 07, пакет 07.17: **самодоготовка разбора подтверждена на рабочем сервере** — ответ за 1,15 с, разбор дошёл до готового без запуска обработчика; расписание не нужно. Найдена причина невидимых стилей: `main.css` подключался без версии и брался из кэша браузера — теперь `asset()` добавляет время изменения файла ко всем стилям и скриптам. Оформление свёрнутого разбора приведено к виду блока «Подробное сравнение».
- Управление знаниями: локальный Graphify имеет обязательную freshness-проверку перед архитектурным query; после 00K граф `STALE`, потому что обновление требует явного разрешения на передачу изменённых исходников внешнему LLM. В этом пакете граф не используется как evidence.
- Governance 00F: старт сессии теперь требует только status, phase активного пакета и чистое понимание Git-state; остальные документы читаются по типу задачи и явно отражаются в отчёте.
- Governance 00G–00H: `STATUS.md` стал единственной оперативной панелью, `CHECKPOINT.md` — только протоколом; исходный audit-plan 2026-08-15 перенесён в архив, а рабочей навигацией по findings остаётся traceability.
- Governance 00I: четыре text-contract test миграций заменяются одним integration test фактически применённой схемы; список маршрутов для документации читается из роутера.
- Governance 00J: по D-032 удаляются неподключённые AI-consent и country/crisis scaffolding; cleanup-миграция удаляет их таблицы из уже развёрнутых БД, а schema test контролирует итоговое отсутствие.
- Governance 00K: CI разделён на единичный fast gate и условную матрицу DB-тестов. В PR MySQL 5.7/8.0 запускаются только для слоя данных, migrations, зависимостей и CI; каждый push в `main` по-прежнему выполняет полную DB-матрицу перед deployment.
- Ревью от 25.08 ([docs/audit/2026-08-25-project-review.md](../audit/2026-08-25-project-review.md)) подтвердило зелёный gate и выдало приоритизированный план. Выполнено 00L (архив устаревших доков, `composer migrate` в gate) и 04.0G: мёртвый Chart.js-контур снят, вместо него добавлен нативный веб-график совмещённых профилей пары Лазаруса (вариант C — наложенные линии с красными зонами расхождений и тултипами по точкам) на странице результата и на `/pair/{id}`; детальная web-таблица и компактный PDF не менялись (guard-тесты). Release `4775dc4` выложен на staging (PR #25, CI success, smoke прошёл, данные БД не затронуты: 28 сессий/1 пара; rollback `3a2daa8`). Тултипы проверены владельцем на staging 25.08 — работает, пакет 04.0G принят. Решения владельца от 25.08: D-033 — legacy `services/*` заморожен до этапов 06/07 (не удалять); D-034 — подтверждена продуктовая модель платных разборов и купонного потока (уточнения в phase-файлах 06/07, включая историю прохождений при авторизации). Этап 00 закрыт. 04.0H закрыл UX-03 (touch-зоны 24px, контраст WCAG AA с контрактом), этап 04 закрыт. 02.7C удалил legacy IP/UA-колонки и старые значения (D-035) — минимизация технических метаданных завершена. 08.1F: `current` + cleanup-cron настроен владельцем (03:17 ежедневно). 08.1G: restore drill пройден (дамп восстанавливается целиком, сверка строк сошлась), `PRODUCTION_RUNBOOK.md` готов (предусловия, выкладка, откат, go-live чек-лист). 08.1G: restore drill пройден, PRODUCTION_RUNBOOK.md готов; для бэкапов используется ежедневный автоматический бэкап Beget (решение владельца — свой ночной дамп не нужен). 03.1A golden characterization; 03.1B capability registry; 03.1C answer schema validator (`getAnswerSchema()` + схема-ориентированный `AnswerValidator`); 03.2 renderer contract (`ResultSectionRenderer`, декларативный `pairChartData()`, `RendererContractTest`, baseline 147); 03.3 демо-модуль `tests/fixtures/demo-wellbeing/` (proof «новый тест без изменений ядра»), удаление мёртвого хука `getResultTemplate()`, актуальное руководство creating-new-test.md. WP3–WP7 этапа 03 закрыты (WP5 выполнен без адаптера — см. phase-файл); до закрытия этапа остался walkthrough доки в чистом окружении. Активные фронты: 03 (завершение), 02 и 08 (остались владельческие пункты: legal review, мониторинг, staging acceptance).
- 04.0F: подтверждён дефект PDF результата Лазаруса с парным сравнением — компактный PDF-layout не получал `is_pdf`, поэтому в документ попадала широкая web-таблица. Исправление из PR #23 выложено как `3a2daa8`: pair result PDF использует компактную таблицу на A4 landscape без разрыва строк; scoring не менялся. Автоматические CI/deployment smoke прошли, нужна ручная проверка PDF владельцем.
- Последний выложенный пакет: 04.0F `3a2daa8` — общий PDF результата Лазаруса с парным сравнением фактически переведён на compact landscape-компоновку. Полная CI-матрица PHP 8.3/MySQL 5.7 и 8.0, миграция staging и внешний HTTPS smoke — success; rollback `5da9ab5` готов.
- Baseline commit: `6c51cc3` (`main` на начало аудита).
- Состояние продукта: quality gates, dependency safety, legacy payment containment, CSRF и границы result-token улучшены; публичная продажа пока не готова к запуску.
- Последние завершённые work packages: CI для PHP 8.3 (`0c91adf`), Linux-совместимый autoload Лазаруса (`b82347e`), CSRF enforcement (`e42eb89`) и прозрачный протокол статусов (`ed3d896`).
- 01.4 принят в `main`: lookup результата разделён с pair-reference; GitHub Actions [31904747962](https://github.com/dmitryturin-art/psytest-platform/actions/runs/31904747962) — success. 01.5A route/session integrity и 01.5B server validation подтверждены CI. 01.5C устранил конфликт `uq_partner_token`; release gate снова зелёный.

## Готовность этапов

| Этап | Состояние | Прогресс/условие перехода |
|---|---|---|
| 00 | Завершён | закрыт 25.08: 00A–00M выполнены, находки ревью от 25.08 применены (00L/00M), baseline воспроизводим |
| 01 | Завершён | containment/security boundaries, validation, web-root hygiene и PAIR-01 подтверждены CI |
| 02 | В работе | lifecycle, BDI safety, factual privacy copy, реестр методик, owner dashboard и минимизация метаданных (02.7C/D-035) реализованы. Нужны staging/pilot evidence, automated BDI browser coverage и legal review |
| 03 | Завершён | 03.1A–03.1C characterization/registry/schema (25.08, PR #28–#30); 03.2 renderer contract (PR #33); 03.3 demo module + dead hook removal; 03.4 walkthrough в чистом окружении выполнен, 4 дефекта руководства исправлены. Закрыт 26.08 решением владельца: все пять exit criteria выполнены |
| 04 | Завершён | закрыт 25.08: UX-01..03 закрыты (04.0G pair chart принят владельцем, 04.0H touch/контраст), направление A внедрено; SMIL-график не менялся |
| 05, 09 | Не начаты | открываются по exit criteria предыдущих этапов; исследования допустимы раньше без release |
| 07 | Следующий рабочий этап | по D-037 идёт раньше 06: расширенный ИИ-разбор сначала доводится до работающего состояния бесплатно (кабинет, генерация, доставка), гейт оплаты выключен на уровне продукта |
| 06 | Не начат, после 07 | по D-037 подключается как переключатель доступа к уже работающему разбору: заказы, купоны, entitlement, фискальная часть |
| 08 | На проверке владельца (staging) | публичный HTTPS staging работает на `4775dc4`+; cleanup-cron и restore drill готовы (08.1F/G), rollback готов; production не начат |

## Baseline, обнаруженный аудитом

| Проверка | Результат на 2026-08-15 | Интерпретация |
|---|---|---|
| PHPUnit | 103 tests, 1153 assertions — pass | Полезная база; security packages добавляют целевые регрессии, browser flow ещё не покрыт полностью |
| Composer validate | pass | `composer.json` синтаксически корректен |
| PHP syntax/style | pass | Не доказывает корректность поведения |
| PHPStan | pass, baseline 148 | Новые ошибки запрещены; baseline нужно постепенно уменьшать |
| Architecture check | pass: 5 модулей, шаблоны и статика | project root исправлен и покрыт узким regression-тестом |
| PHPStan baseline guard | pass: ровно 148 entries | `composer baseline:check` запрещает незаметный рост baseline |
| Dependency audit | pass: `dompdf` 3.1.6, audit clean | `DEP-01` закрыт в `7272e51`; lock воспроизводим для PHP 8.3 |
| Browser smoke | пройден частично | BDI progress и пустая sticky navigation исправлены на desktop/mobile; остаются accessibility и responsive-дефекты |

Свежие команды и точный вывод добавляются в [WORKLOG.md](WORKLOG.md); эта таблица не заменяет повторный baseline run.

## Активные риски

1. Legacy payment endpoints безопасно retired, но новый YooKassa/AI flow ещё не спроектирован и не реализован.
3. BDI item 9 создаёт server-side signal; 02.2B публикует generic notice без country/resource reader. Desktop/mobile browser QA и PHP 8.3/MySQL CI пройдены; automated browser coverage остаётся отдельным пакетом.
4. 02.4A устраняет ложные public privacy/delete claims; 00C приводит local developer docs к коду, но production runbook и legal review ещё не завершены.
5. Дополнительные шкалы SMIL: реализовано 23 при заявленных 200. Обзор 05.0 нашёл источник — ключи и нормы (отдельно для мужчин и женщин) лежат в Приложении руководства Собчик, но это скан без текстового слоя, поэтому перенос требует OCR с контрольной суммой по числу пунктов. До расширения нужно сверить ключи уже реализованных 23 шкал с источником: расхождение там было бы дефектом работающего расчёта.
6. Права на русские формы методик. Владелец установил (D-042), что использование методик бесплатно, а права требуются для продажи самой методики; продуктовая модель продаёт только ИИ-интерпретацию, а прохождение и базовые результаты бесплатны навсегда. Это снимает блокировку платного разбора как таковую, но **не закрывает два отдельных вопроса о воспроизведении**: (а) публикация текстов пунктов на публичном сайте — это распространение адаптации независимо от платности; (б) перенос ключей дополнительных шкал из руководства Собчик в продукт — воспроизведение существенной части авторского труда. Происхождение конкретных редакций по-прежнему не подтверждено документами: наборы брались со стороннего сайта, а не у правообладателя.
7. Два PDF результатов присутствуют в старой Git history; в актуальной ветке они не отслеживаются. Владелец подтвердил, что они обезличены, и решил не переписывать историю.
8. `test.23time.ru` работает по HTTPS; Basic Auth не используется по D-029. До production используются только synthetic/добровольно введённые данные.
9. Staging DB работает на MySQL 5.7.21. 08.1C нашёл и устранил несовместимый implicit TIMESTAMP default; чистые MySQL 5.7 и 8.0 теперь обязательны в CI.

Полный список и владельцы закрытия: [AUDIT_TRACEABILITY.md](AUDIT_TRACEABILITY.md).

## Следующие пять действий

1. Решение владельца о платной модели для сравнения с его ручным способом: аккаунт не на бесплатном тарифе, отчёт стоит доли цента, бесплатные модели показали негодное качество.
1a. Сразу после WP2 — **ранняя проверка формата входа**: владелец всегда отправлял модели скриншот/PDF с графиком и сырыми данными, платформа отправляет JSON. Сравнить на одних данных отчёт из структурированной нагрузки с отчётом, который владелец получает вручную. Обязательно до вложений в WP5–WP7.
2. Этап 07, WP1: адаптер провайдера — после владельческих решений о провайдере/модели и потолке расходов на один разбор.
3. Этап 07, WP3 остаток: структурированная нагрузка для СМИЛ (вместе с промптом владельца) и snapshot расчёта для воспроизводимости отчёта.
4. Legal review и подтверждение происхождения/прав методик (риск №6) — блокер **платной** выдачи; бесплатный контур этапа 07 им не блокируется.
5. Короткий бесплатный пилот (exit этапа 02) и ссылки в results/PDF из [issue #9](https://github.com/dmitryturin-art/psytest-platform/issues/9).

## Решения владельца, нужные сейчас

- Финальная приёмка staging: полный парный PDF Лазаруса (04.0F follow-up) и основные flows направления A.
- Legal review/provenance методик: без подтверждённых прав платная интерпретация не открывается даже после этапов 06/07.
- Для воспроизводимых сравнений: пиновать конкретную бесплатную модель вместо маршрутизатора `openrouter/free` (он выбирает исполнителя сам). Ключ внесён, каталог доступен: 20 бесплатных моделей.
- До первого реального клиента: платная модель с политикой без обучения на запросах либо явное согласие на условия бесплатных моделей.
- Структурированная нагрузка для СМИЛ (промпт получен и опубликован; сама нагрузка `aiReportContext()` для СМИЛ ещё не написана). Первый релиз ИИ-разборов: только СМИЛ и Лазарус.

## Checkpoint

[CHECKPOINT.md](CHECKPOINT.md) содержит только протокол команды «сделай checkpoint»; актуальное состояние находится выше в этом файле.

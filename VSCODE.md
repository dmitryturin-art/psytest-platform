# 🚀 Запуск проекта из VS Code

## Шаг 1: Установите PHP

### macOS - через Homebrew (рекомендуется)

1. Откройте терминал в VS Code (`Ctrl + ~`)

2. Установите Homebrew (если нет):
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

3. Установите PHP:
```bash
brew install php@8.2
```

4. Проверьте установку:
```bash
php -v
```

### macOS - через MAMP (проще всего)

1. Скачайте MAMP: https://www.mamp.info/en/downloads/
2. Установите и запустите MAMP
3. PHP будет доступен автоматически

### Windows

1. Скачайте PHP: https://windows.php.net/download/
2. Распакуйте в `C:\php`
3. Добавьте `C:\php` в PATH
4. Или установите XAMPP: https://www.apachefriends.org/

---

## Шаг 2: Установите Composer

### macOS
```bash
brew install composer
```

### Windows
Скачайте установщик: https://getcomposer.org/download/

---

## Шаг 3: Настройте VS Code

### Установите расширения

VS Code предложит установить рекомендуемые расширения для PHP. Нажмите **"Установить все"**.

Или установите вручную:
1. Откройте Extensions (`Cmd+Shift+X`)
2. Найдите и установите:
   - **PHP Intelephense** (автодополнение)
   - **PHP Debug** (отладка)

---

## Шаг 4: Запустите проект

### Вариант A: Через Tasks (рекомендуется)

1. Нажмите `Cmd+Shift+P` → "Tasks: Run Task"
2. Выберите задачу:

| Задача | Описание |
|--------|----------|
| `Запустить PHP сервер` | Запускает встроенный сервер |
| `Установить зависимости Composer` | `composer install` |
| `Инициализировать базу данных` | Создаёт таблицы |
| `Проверить архитектуру` | Тест без БД |

### Вариант B: Через терминал

1. Откройте терминал в VS Code (`Ctrl + ~`)
2. Выполните команды:

```bash
# Установка зависимостей
composer install

# Запуск сервера разработки
php -S localhost:8000 -t public
```

---

## Шаг 5: Откройте в браузере

Перейдите на: **http://localhost:8000/tests**

---

## 🔧 Настройка базы данных

### Вариант 1: Docker (проще всего)

В терминале VS Code:
```bash
# Запустить MySQL
docker run --name psytest-mysql -e MYSQL_ROOT_PASSWORD=secret -e MYSQL_DATABASE=psytest -p 3306:3306 -d mysql:8

# Инициализировать БД
php bin/install-db.php
```

### Вариант 2: Локальный MySQL

```bash
# macOS
brew install mysql
brew services start mysql

# Инициализировать БД
php bin/install-db.php
```

### Вариант 3: MAMP

1. Запустите MAMP
2. Откройте `http://localhost:8888/phpMyAdmin`
3. Создайте базу `psytest`
4. Импортируйте `database/schema.sql`

---

## 🐛 Отладка в VS Code

### Запуск с отладкой

1. Откройте файл для отладки (например, `public/index.php`)
2. Нажмите `F5` или выберите "Run and Debug"
3. Выберите "Launch current file"

### Точки останова

1. Кликните слева от номера строки (появится красная точка)
2. Запустите отладку
3. Выполнение остановится в этой точке

---

## 📋 Чек-лист запуска

- [ ] PHP установлен (`php -v`)
- [ ] Composer установлен (`composer --version`)
- [ ] Зависимости установлены (`composer install`)
- [ ] База данных настроена
- [ ] `.env` создан из `.env.example`
- [ ] Сервер запущен (`php -S localhost:8000 -t public`)
- [ ] Страница открывается (`http://localhost:8000/tests`)

---

## ⚡ Быстрый старт (команды для терминала)

```bash
# 1. Перейдите в проект
cd /Users/dmitrijturin/VibeCoding/hyptest

# 2. Установите зависимости
composer install

# 3. Скопируйте конфиг
cp .env.example .env

# 4. Если есть Docker - запустите MySQL
docker run --name psytest-mysql -e MYSQL_ROOT_PASSWORD=secret -e MYSQL_DATABASE=psytest -p 3306:3306 -d mysql:8

# 5. Инициализируйте БД
php bin/install-db.php

# 6. Запустите сервер
php -S localhost:8000 -t public

# 7. Откройте http://localhost:8000/tests
```

---

## 🆘 Если что-то не работает

### "php: command not found"
- PHP не установлен или не в PATH
- Перезапустите VS Code после установки PHP

### "Class not found"
```bash
composer dump-autoload
```

### "Database connection failed"
- Проверьте, что MySQL запущен
- Проверьте credentials в `.env`

### Ошибки в терминале
- Откройте панель Problems (`Cmd+Shift+M`)
- Посмотрите логи в `storage/logs/`

---

## 📚 Полезные команды VS Code

| Команда | Действие |
|---------|----------|
| `Cmd+Shift+P` | Command Palette |
| `Ctrl + ~` | Открыть терминал |
| `Cmd+Shift+B` | Запустить Task |
| `F5` | Запустить отладку |
| `Cmd+P` | Быстрый переход к файлу |
| `Cmd+Shift+F` | Поиск по проекту |

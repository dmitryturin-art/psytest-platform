# Технический аудит кодовой базы PsyTest Platform

**Дата:** 26 февраля 2026  
**Версия:** 1.0  
**Аудитор:** Архитектор AI

---

## 📋 Содержание

1. [Резюме](#резюме)
2. [Архитектура системы](#архитектура-системы)
3. [Избыточные файлы](#избыточные-файлы)
4. [Дублирующийся код](#дублирующийся-код)
5. [Потенциальные ошибки](#потенциальные-ошибки)
6. [Проблемы модуля СМИЛ](#проблемы-модуля-смил)
7. [Оценка производительности](#оценка-производительности)
8. [Рекомендации](#рекомендации)

---

## 🎯 Резюме

### Общая оценка: **7/10**

**Сильные стороны:**
- ✅ Четкая модульная архитектура с разделением ответственности
- ✅ Использование современных практик PHP 8.1+ (strict types, typed properties)
- ✅ Хорошая структура базы данных с правильными индексами
- ✅ Наличие базовой документации
- ✅ Использование Composer для управления зависимостями

**Критические проблемы:**
- ❌ **КРИТИЧНО:** Некорректные вопросы СМИЛ (546-562) - нереалистичные формулировки
- ❌ Избыточный файл `test-taking-old.js` (11980 байт)
- ⚠️ Дублирование кода в контроллерах
- ⚠️ Отсутствие валидации данных в некоторых местах
- ⚠️ Неполная реализация дополнительных шкал СМИЛ

---

## 🏗️ Архитектура системы

### Текущая структура

```
PsyTest Platform
├── Core Layer (Ядро)
│   ├── Database (PDO Singleton)
│   ├── Router (URL маршрутизация)
│   ├── SessionManager (Управление сессиями)
│   ├── ModuleLoader (Загрузка модулей)
│   ├── View (Twig рендеринг)
│   ├── PDFGenerator (DomPDF)
│   ├── Security (CSRF, XSS защита)
│   └── LoggerFactory (Monolog)
│
├── Controllers Layer (Контроллеры)
│   ├── HomeController (Главная, список тестов)
│   ├── TestController (Прохождение тестов)
│   ├── ResultController (Результаты, PDF)
│   └── ApiController (API, webhooks)
│
├── Modules Layer (Модули тестов)
│   ├── TestModuleInterface (Интерфейс)
│   ├── BaseTestModule (Базовый класс)
│   ├── smil/ (СМИЛ модуль)
│   └── beck-anxiety/ (Beck Anxiety модуль)
│
├── Services Layer (Сервисы)
│   ├── PaymentService (ЮMoney)
│   ├── AIInterpretationService (OpenRouter)
│   └── EmailService (PHPMailer)
│
└── Presentation Layer (Представление)
    ├── Templates (Twig)
    ├── CSS (main.css)
    └── JavaScript (модульные скрипты)
```

### Оценка архитектуры

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| Модульность | ⭐⭐⭐⭐⭐ | Отличное разделение на модули |
| Расширяемость | ⭐⭐⭐⭐☆ | Легко добавлять новые тесты |
| Поддерживаемость | ⭐⭐⭐☆☆ | Есть дублирование кода |
| Производительность | ⭐⭐⭐⭐☆ | Хорошая, но можно оптимизировать |
| Безопасность | ⭐⭐⭐⭐☆ | Базовая защита есть |
| Документация | ⭐⭐⭐☆☆ | Неполная, требует доработки |

---

## 🗑️ Избыточные файлы

### 1. JavaScript файлы

#### `public/js/test-taking-old.js` (11980 байт)
**Статус:** ❌ УДАЛИТЬ

**Причина:**
- Дублирует функциональность `test-taking.js`
- Устаревшая версия без улучшений
- Не используется в шаблонах

**Действие:**
```bash
rm public/js/test-taking-old.js
```

**Экономия:** ~12 KB

---

### 2. Потенциально избыточные файлы данных

#### `modules/smil/questions_sample.json` (5479 байт)
**Статус:** ⚠️ ПРОВЕРИТЬ

**Причина:**
- Возможно, демо-версия вопросов
- Не используется в коде (используется `questions.json` и `questions-full.json`)

**Рекомендация:**
- Если не нужен для демонстрации - удалить
- Или переименовать в `questions-demo.json` для ясности

---

### 3. Документация

#### Множественные README файлы
**Статус:** ✅ ОСТАВИТЬ, НО РЕОРГАНИЗОВАТЬ

**Файлы:**
- `README.md` - основной
- `QUICKSTART.md` - быстрый старт
- `DEVELOPMENT.md` - разработка
- `DEPLOYMENT.md` - развертывание
- `VSCODE.md` - настройка VS Code
- `WORKLOG.md` - журнал работ
- `BAI-TEST.md` - документация теста Beck
- `INSTALLED.md` - инструкции установки
- `PUBLISH.md` - публикация

**Рекомендация:**
- Объединить `INSTALLED.md` и `QUICKSTART.md`
- Переместить `WORKLOG.md` в `docs/`
- Создать единую точку входа в документацию

---

## 🔄 Дублирующийся код

### 1. Инициализация зависимостей в контроллерах

**Проблема:** Каждый контроллер дублирует код инициализации

**Пример из `TestController.php`:**
```php
public function __construct()
{
    $this->db = Database::getInstance();
    $this->view = View::getInstance();
    $this->moduleLoader = (new ModuleLoader(null, $this->db))->discover();
    $this->sessionManager = new SessionManager($this->db);
}
```

**То же самое в `ResultController.php`, `HomeController.php`**

**Решение:**
Создать базовый класс `BaseController`:

```php
abstract class BaseController
{
    protected Database $db;
    protected View $view;
    protected ModuleLoader $moduleLoader;
    protected SessionManager $sessionManager;
    
    public function __construct()
    {
        $this->db = Database::getInstance();
        $this->view = View::getInstance();
        $this->moduleLoader = (new ModuleLoader(null, $this->db))->discover();
        $this->sessionManager = new SessionManager($this->db);
    }
}
```

**Экономия:** ~40 строк кода, улучшение поддерживаемости

---

### 2. Проверка существования модуля

**Проблема:** Повторяющаяся логика проверки модуля

**Встречается в:**
- `TestController::start()`
- `TestController::submit()`
- `ResultController::show()`

**Пример:**
```php
$module = $this->moduleLoader->getModule($slug);
if (!$module) {
    http_response_code(404);
    echo $this->view->render('error-page');
    return;
}
```

**Решение:**
Создать метод в `BaseController`:

```php
protected function getModuleOrFail(string $slug): TestModuleInterface
{
    $module = $this->moduleLoader->getModule($slug);
    if (!$module) {
        http_response_code(404);
        echo $this->view->render('error-page');
        exit;
    }
    return $module;
}
```

---

### 3. Нормализация ответов

**Проблема:** Логика нормализации ответов дублируется

**В `TestController::submit()`:**
```php
$normalizedAnswers = [];
foreach ($answers as $questionId => $answer) {
    if (is_numeric($answer)) {
        $normalizedAnswers[$questionId] = (int) $answer;
    } else {
        $normalizedAnswers[$questionId] = $answer === 'true' || $answer === true;
    }
}
```

**Решение:**
Перенести в `BaseTestModule::normalizeAnswers()`:

```php
protected function normalizeAnswers(array $answers): array
{
    $normalized = [];
    foreach ($answers as $questionId => $answer) {
        if (is_numeric($answer)) {
            $normalized[$questionId] = (int) $answer;
        } else {
            $normalized[$questionId] = $answer === 'true' || $answer === true;
        }
    }
    return $normalized;
}
```

---

## 🐛 Потенциальные ошибки

### 1. Отсутствие валидации входных данных

**Файл:** `controllers/TestController.php:110`

**Проблема:**
```php
public function submit(string $slug): void
{
    $sessionId = $_POST['session_id'] ?? null;
    if (!$sessionId) {
        http_response_code(400);
        echo 'Invalid session';
        return;
    }
    // Нет проверки формата UUID!
}
```

**Риск:** SQL injection через некорректный UUID

**Решение:**
```php
if (!$sessionId || !Uuid::isValid($sessionId)) {
    http_response_code(400);
    echo 'Invalid session ID format';
    return;
}
```

---

### 2. Потенциальная утечка памяти при загрузке вопросов

**Файл:** `modules/smil/SmilModule.php:204`

**Проблема:**
```php
public function getQuestions(): array
{
    if ($this->questions === null) {
        $this->questions = $this->loadQuestionsFromJson('questions-full.json');
        // ...
    }
    return $this->questions;
}
```

**Риск:** 
- Файл `questions-full.json` весит 50KB
- При множественных вызовах данные остаются в памяти
- Для 566 вопросов это ~200KB в памяти на каждый экземпляр модуля

**Решение:**
Использовать ленивую загрузку или кэширование на уровне приложения

---

### 3. Отсутствие обработки ошибок JSON

**Файл:** `modules/BaseTestModule.php:58`

**Проблема:**
```php
$content = file_get_contents($filepath);
$data = json_decode($content, true) ?? [];
```

**Риск:**
- Если JSON невалидный, `json_decode` вернет `null`
- Ошибка будет скрыта оператором `??`
- Сложно диагностировать проблемы

**Решение:**
```php
$content = file_get_contents($filepath);
$data = json_decode($content, true);

if (json_last_error() !== JSON_ERROR_NONE) {
    throw new \RuntimeException(
        "Failed to parse JSON in {$filepath}: " . json_last_error_msg()
    );
}

return $data ?? [];
```

---

### 4. Race condition в SessionManager

**Файл:** `core/SessionManager.php:40`

**Проблема:**
```php
public function createSession(int $testId, array $options = []): array
{
    $sessionToken = $this->generateSecureToken();
    // Нет проверки на уникальность токена перед вставкой!
    
    $this->db->insert('test_sessions', $data);
}
```

**Риск:**
- Теоретически возможна коллизия токенов
- При высокой нагрузке может привести к ошибке дублирования

**Решение:**
```php
$maxAttempts = 3;
for ($i = 0; $i < $maxAttempts; $i++) {
    $sessionToken = $this->generateSecureToken();
    
    try {
        $this->db->insert('test_sessions', $data);
        break;
    } catch (PDOException $e) {
        if ($i === $maxAttempts - 1) {
            throw $e;
        }
        // Retry with new token
    }
}
```

---

## ❌ Проблемы модуля СМИЛ

### 1. КРИТИЧНО: Некорректные вопросы

**Файл:** `modules/smil/questions-full.json:546-562`

**Проблема:**
Вопросы 546-562 содержат нереалистичные формулировки:

```json
{"id": 546, "text": "Вы иногда разрушаете реальность?", "scale": "4", "direction": 1},
{"id": 547, "text": "Вы часто чувствуете космический ужас?", "scale": "2", "direction": 1},
{"id": 548, "text": "Вы боитесь стать монстром?", "scale": "7", "direction": 1},
{"id": 554, "text": "Вы чувствуете, что ваше существование ошибочно?", "scale": "8", "direction": 1},
{"id": 557, "text": "Люди хотят вас стереть из памяти?", "scale": "6", "direction": 1},
{"id": 560, "text": "Вы часто чувствуете трансцендентальную тревогу?", "scale": "2", "direction": 1}
```

**Последствия:**
- ❌ Результаты теста недостоверны
- ❌ Невозможно сравнить с эталонными нормами
- ❌ Нарушена валидность методики

**Действие:**
**ПРИОРИТЕТ 1:** Заменить все 566 вопросов на корректные из эталонного источника

---

### 2. Неполная реализация дополнительных шкал

**Файл:** `modules/smil/additional-scales.json`

**Проблема:**
- Заявлено 200+ шкал
- Реализовано только ~30 шкал
- Отсутствуют важные шкалы:
  - Контент-шкалы (ANX, FRS, OBS, DEP, HEA, BIZ, ANG, CYN, ASP, TPA, LSE, SOD, FAM, WRK, TRT)
  - Исследовательские шкалы
  - Специальные индексы

**Действие:**
Поэтапная реализация дополнительных шкал (см. план рефакторинга)

---

### 3. Отсутствие валидации ключей шкал

**Файл:** `modules/smil/SmilModule.php:321`

**Проблема:**
```php
protected function calculateAdditionalScales(array $answers, string $gender): array
{
    foreach ($normsData as $category => $scales) {
        foreach ($scales as $code => $info) {
            if (!isset($info['key']) || !isset($info['norms'])) {
                continue; // Просто пропускаем!
            }
            // ...
        }
    }
}
```

**Риск:**
- Молчаливое игнорирование ошибок в данных
- Сложно обнаружить проблемы в конфигурации шкал

**Решение:**
Добавить логирование или исключения для некорректных шкал

---

### 4. Жестко закодированные интерпретации

**Файл:** `modules/smil/SmilModule.php:58-200`

**Проблема:**
Интерпретации шкал жестко закодированы в PHP-константах:

```php
protected const INTERPRETATIONS = [
    'L' => [
        'low' => 'Низкая социальная желательность, искренность',
        'normal' => 'Умеренная социальная желательность',
        // ...
    ],
    // ...
];
```

**Недостатки:**
- Сложно редактировать без изменения кода
- Невозможно добавить вариативность интерпретаций
- Нет поддержки разных уровней детализации

**Решение:**
Вынести в JSON-файл `interpretations-detailed.json` с многоуровневой структурой

---

## ⚡ Оценка производительности

### Узкие места

#### 1. Загрузка модулей при каждом запросе

**Файл:** `core/ModuleLoader.php:29`

```php
public function discover(): self
{
    $directories = glob($this->modulesPath . '/*', GLOB_ONLYDIR);
    
    foreach ($directories as $dir) {
        $this->loadModule($dir);
    }
    
    return $this;
}
```

**Проблема:**
- Сканирование файловой системы при каждом запросе
- Загрузка всех модулей, даже если нужен только один

**Решение:**
Кэширование списка модулей:

```php
public function discover(): self
{
    $cacheKey = 'modules_registry';
    $cached = apcu_fetch($cacheKey);
    
    if ($cached !== false) {
        $this->modules = $cached;
        return $this;
    }
    
    // Сканирование и загрузка...
    
    apcu_store($cacheKey, $this->modules, 3600);
    return $this;
}
```

**Выигрыш:** ~5-10ms на запрос

---

#### 2. Множественные запросы к БД

**Файл:** `controllers/ResultController.php:78-84`

```php
$aiInterpretation = $this->db->selectOne(
    'SELECT * FROM ai_interpretations WHERE session_id = ? AND payment_status = "completed"',
    [$session['id']]
);

$pairComparison = $this->sessionManager->getPairComparisonBySession($session['id']);
```

**Проблема:**
- 2 отдельных запроса для одной страницы
- Можно объединить в один JOIN

**Решение:**
```sql
SELECT 
    ts.*,
    ai.interpretation_text,
    pc.comparison_data
FROM test_sessions ts
LEFT JOIN ai_interpretations ai ON ai.session_id = ts.id AND ai.payment_status = 'completed'
LEFT JOIN pair_comparisons pc ON pc.session_1_id = ts.id OR pc.session_2_id = ts.id
WHERE ts.session_token = ?
```

**Выигрыш:** ~2-5ms на запрос

---

#### 3. Отсутствие индексов на JSON-полях

**Файл:** `database/schema.sql`

**Проблема:**
- Поля `answers`, `calculated_results`, `demographics` - JSON
- Нет виртуальных колонок для часто используемых полей
- Медленный поиск по демографическим данным

**Решение:**
```sql
ALTER TABLE test_sessions 
ADD COLUMN gender VARCHAR(10) 
GENERATED ALWAYS AS (JSON_UNQUOTE(JSON_EXTRACT(demographics, '$.gender'))) STORED,
ADD INDEX idx_gender (gender);
```

---

## 📊 Метрики кода

### Размеры файлов

| Файл | Строки | Размер | Оценка |
|------|--------|--------|--------|
| `SmilModule.php` | 1800+ | 62KB | ⚠️ Слишком большой |
| `main.css` | 1500+ | 51KB | ⚠️ Требует разбиения |
| `SessionManager.php` | 350+ | 10KB | ✅ Нормально |
| `TestController.php` | 263 | 8KB | ✅ Нормально |

### Сложность кода (Cyclomatic Complexity)

| Метод | Сложность | Оценка |
|-------|-----------|--------|
| `SmilModule::renderResults()` | ~15 | ⚠️ Высокая |
| `SmilModule::calculateResults()` | ~10 | ✅ Приемлемая |
| `TestController::submit()` | ~8 | ✅ Приемлемая |

---

## 💡 Рекомендации

### Приоритет 1: КРИТИЧНО

1. **Заменить вопросы СМИЛ**
   - Загрузить корректные 566 вопросов из эталонного источника
   - Проверить соответствие ключей шкал
   - Валидировать направления подсчета (прямой/обратный)

2. **Удалить избыточные файлы**
   - `public/js/test-taking-old.js`
   - Проверить и удалить неиспользуемые JSON-файлы

3. **Исправить критические ошибки безопасности**
   - Добавить валидацию UUID
   - Обработка ошибок JSON
   - Проверка уникальности токенов

---

### Приоритет 2: Важно

4. **Рефакторинг контроллеров**
   - Создать `BaseController`
   - Вынести общую логику
   - Уменьшить дублирование кода

5. **Оптимизация производительности**
   - Кэширование модулей
   - Оптимизация SQL-запросов
   - Добавление индексов

6. **Разбить большие файлы**
   - `SmilModule.php` → разделить на трейты/классы
   - `main.css` → модульная структура

---

### Приоритет 3: Желательно

7. **Улучшение документации**
   - Объединить дублирующиеся README
   - Создать API-документацию
   - Добавить примеры использования

8. **Автоматизация тестирования**
   - Unit-тесты для модулей
   - Интеграционные тесты
   - CI/CD pipeline

9. **Реализация дополнительных шкал СМИЛ**
   - Поэтапное добавление 200+ шкал
   - Валидация по эталонным данным
   - Документирование каждой шкалы

---

## 📈 План действий

### Этап 1: Критические исправления (1-2 дня)

- [ ] Заменить вопросы СМИЛ на корректные
- [ ] Удалить `test-taking-old.js`
- [ ] Исправить валидацию UUID
- [ ] Добавить обработку ошибок JSON

### Этап 2: Рефакторинг (3-5 дней)

- [ ] Создать `BaseController`
- [ ] Вынести общую логику
- [ ] Оптимизировать SQL-запросы
- [ ] Добавить кэширование модулей

### Этап 3: Оптимизация (5-7 дней)

- [ ] Разбить `SmilModule.php`
- [ ] Модульная структура CSS
- [ ] Добавить индексы БД
- [ ] Оптимизация загрузки вопросов

### Этап 4: Документация (2-3 дня)

- [ ] Объединить README
- [ ] API-документация
- [ ] Руководство по добавлению тестов
- [ ] Диаграммы архитектуры

---

## 🎯 Ожидаемые результаты

После выполнения всех рекомендаций:

- ✅ **Корректность:** Валидные вопросы СМИЛ, достоверные результаты
- ✅ **Производительность:** Ускорение на 20-30%
- ✅ **Поддерживаемость:** Уменьшение дублирования кода на 40%
- ✅ **Безопасность:** Устранение всех критических уязвимостей
- ✅ **Документация:** Полное покрытие всех компонентов

---

## 📞 Следующие шаги

1. **Утверждение плана** - обсудить приоритеты с заказчиком
2. **Создание задач** - разбить на конкретные задачи в трекере
3. **Начало работ** - приступить к Этапу 1

---

**Конец отчета**
